
const MAXIMUM_SESSIONS_REACHED = {
  type: 'ERROR',
  message: 'MAXIMUM_SESSIONS_REACHED',
};

const WRONG_PARMS_FOR_JOIN = {
  type: 'ERROR',
  message: 'WRONG_PARMS_FOR_JOIN',
};

const SESSION_NOT_FOUND = {
  type: 'ERROR',
  message: 'SESSION_NOT_FOUND',
};

const WRONG_PARMS_FOR_TURN = {
  type: 'ERROR',
  message: 'WRONG_PARMS_FOR_TURN',
};

const NOT_USERS_TURN = {
  type: 'ERROR',
  message: 'NOT_USERS_TURN',
};

const POINT_ALREADY_PRESENT = {
  type: 'ERROR',
  message: 'POINT_ALREADY_PRESENT',
};

const WRONG_DATA_FORMAT = {
  type: 'ERROR',
  message: 'WRONG_DATA_FORMAT',
};

module.exports = {
  MAXIMUM_SESSIONS_REACHED,
  WRONG_PARMS_FOR_JOIN,
  SESSION_NOT_FOUND,
  WRONG_PARMS_FOR_TURN,
  NOT_USERS_TURN,
  POINT_ALREADY_PRESENT,
  WRONG_DATA_FORMAT,
};
